using UnityEngine;

namespace BlueRacconGames.Hit.Data
{
    [CreateAssetMenu(fileName = nameof(HitTargetType), menuName = nameof(Hit) + "/" + nameof(Hit.Data) + "/" + nameof(HitTargetType))]
    public class HitTargetType : ScriptableObject
    {

    }
}